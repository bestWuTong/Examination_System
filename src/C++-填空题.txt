在C++中，使用关键字（ ）来定义类。
△class△

类的三大基本特性是：封装、（ ）和多态。
△继承△

用于初始化对象的数据成员的特殊成员函数叫做（ ）。
△构造函数△

在类中，默认访问权限是（ ）。
△private△

通过将类的数据成员声明为（ ），可以实现信息隐藏，使其在类外不可直接访问。
△private△

与类同名且没有返回类型的函数是（ ）。
△构造函数△

当一个对象被销毁时，自动调用的成员函数是（ ）。
△析构函数△

如果用户没有定义任何构造函数，编译器会自动提供一个（ ）构造函数。
△默认△

用于在创建对象时，用一个已存在的同类型对象对其进行初始化的特殊构造函数叫做（ ）构造函数。
△拷贝△

在构造函数后使用冒号和初始化列表，是为了提高效率并初始化（ ）成员。
△const△

C++中，派生类继承基类使用符号（ ）。
△冒号△

在公有继承中，基类的public成员在派生类中保持为（ ），基类的protected成员在派生类中保持为（ ）。
（两个答案之间用“、”分隔）
△public、protected△

如果派生类的构造函数没有显式调用基类的构造函数，编译器会自动调用基类的（ ）构造函数。
△默认△

当一个类被声明为 class B : public A 时，A 是 B 的（ ），B 是 A 的（ ）。
（两个答案之间用“、”分隔）
△基类、派生类△

在继承体系中，使用（ ）函数可以实现运行时多态。
△虚△

通过基类的指针或引用来调用派生类重写函数的现象，称为（ ）。
△多态△

在基类中，使用关键字（ ）声明的函数称为虚函数。
△virtual△

如果一个类包含纯虚函数，那么这个类被称为（ ）。
△抽象类△

纯虚函数的声明是在函数声明的后面加上（ ）。
△=0△

使用 cout 和 cin 对象需要包含（ ）头文件。
△iostream△

用于允许同名函数处理不同类型数据的机制是（ ）。
△函数重载△

在类内部，this 指针是一个指向（ ）的指针。
△当前对象△

使用（ ）运算符动态分配内存，而使用（ ）运算符来释放它。
（两个答案之间用“、”分隔）
△new、delete△

将运算符视为函数，并通过operator关键字为其重载，这被称为（ ）。
△运算符重载△